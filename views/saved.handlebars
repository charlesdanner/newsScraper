<div class="jumbotron text-center">
  <div class="overlay">
  </div>
  <div class="background-image">
  </div>
</div>

<div id="noteModal" class="modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <button type="button" class="bootbox-close-button white-text close" data-dismiss="modal" aria-hidden="true"
          style="margin-top: -10px;">×</button>
        <div class="bootbox-body">
          <div class="container-fluid text-center">
            <h4 id='modalTitle' class="white-text"></h4>
            <hr class="white-text modalHR">
            <ul id="noteList" class="list-group note-container">
              <li class="list-group-item notesList">No notes for this article yet.</li>
            </ul>
            <textarea id="newNoteTextArea" placeholder="New Note" rows="4" cols="60"></textarea>
            <button id="saveNewNote" class="btn btn-success save">Save Note</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container-fluid article-container" id="articleContainer">
  {{#each article}}
  <div data-_id="{{this._id}}" class="card">
    <div class="card-header">
      <h3 class="container-fluid">
        <a class="article-link saved-article-link" target="_blank" rel="noopener noreferrer" href="{{this.link}}">{{this.title}}</a>
        
      </h3>
      <a class="btn btn-danger delete white-text" id="deleteButton" data-title="{{this.title}}">Delete Article</a>
        <a class="btn save white-text" id="noteButton" data-title="{{this.title}}">Add Note</a>
    </div>
    <div class="card-body">{{this.summary}}</div>
  </div>
  {{/each}}
</div>
  <script src='/assets/js/API.js'></script>
  <script src='/assets/js/savedArticles.js'></script>